<!DOCTYPE html>
<html lang="ro">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact - VanzareMasini</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    body {
      background-color: #000000;
      color: #ffffff;
      min-height: 100vh;
    }

    header {
      background-color: #1e1e1e;
      color: #fff;
      padding: 15px 50px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    header h1 {
      font-size: 26px;
      letter-spacing: 1px;
    }

    nav a {
      color: #ffffff;
      margin-left: 20px;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.3s;
      background: #2a2a2a;
      padding: 8px 16px;
      border-radius: 8px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    nav a:hover {
      background: #ffffff;
      color: #000000;
      transform: translateY(-2px);
      border-color: rgba(255, 255, 255, 0.2);
    }

    .cart-icon {
      font-size: 20px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 8px 16px;
      border-radius: 8px;
      transition: all 0.3s ease;
      background: #2a2a2a;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .cart-icon:hover {
      background: #ffffff;
      color: #000000;
      transform: translateY(-2px);
      border-color: rgba(255, 255, 255, 0.2);
    }

    .contact-container {
      max-width: 900px;
      margin: 60px auto;
      background: linear-gradient(135deg, #1e1e1e 0%, #2a2a2a 100%);
      border-radius: 15px;
      box-shadow: 0 8px 25px rgba(255, 255, 255, 0.1);
      padding: 40px;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    h2 {
      text-align: center;
      margin-bottom: 30px;
      font-size: 32px;
      color: #ffffff;
      text-shadow: 0 2px 4px rgba(255, 255, 255, 0.1);
    }

    .contact-info {
      display: flex;
      flex-wrap: wrap;
      gap: 40px;
      justify-content: space-between;
    }

    .info-box {
      flex: 1;
      min-width: 250px;
    }

    .info-box h3 {
      color: #ffffff;
      margin-bottom: 10px;
      font-weight: 600;
    }

    .info-box p {
      line-height: 1.6;
      color: #cccccc;
    }

    #map {
      width: 100%;
      height: 350px;
      margin-top: 40px;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    form {
      margin-top: 40px;
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    input, textarea {
      padding: 12px 15px;
      border-radius: 8px;
      border: 1px solid #444;
      font-size: 16px;
      outline: none;
      transition: border 0.3s;
      background-color: #2a2a2a;
      color: #ffffff;
    }

    input::placeholder, textarea::placeholder {
      color: #999;
    }

    input:focus, textarea:focus {
      border-color: #ffffff;
    }

    textarea {
      resize: none;
      height: 120px;
    }

    button {
      background-color: #ffffff;
      color: #000000;
      border: none;
      padding: 12px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 18px;
      font-weight: 600;
      transition: all 0.3s;
    }

    button:hover {
      background-color: #cccccc;
      transform: translateY(-2px);
    }

    footer {
      background-color: #111111;
      color: #cccccc;
      text-align: center;
      padding: 20px;
      margin-top: 50px;
      border-top: 1px solid #333333;
    }

    @media (max-width: 700px) {
      .contact-info {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>VanzareMasini</h1>
    <nav>
      <a href="acasa.html">Acasă</a>
      <a href="secondpage.html">Catalog</a>
      <a href="Contact.html">Contact</a>
      <a href="cos.html" class="cart-icon">🛒</a>
    </nav>
  </header>

  <div class="contact-container">
    <h2>Contactează-ne</h2>
    <div class="contact-info">
      <div class="info-box">
        <h3>Adresa</h3>
        <p>Str. Memorandumului nr.18<br>Brașov, România</p>
      </div>
      <div class="info-box">
        <h3>Telefon</h3>
        <p>+40 773 781 228 - Mada</p>
        <p>+40 720 466 633 - Gabi</p>
      </div>
      <div class="info-box">
        <h3>Email</h3>
        <p>contact@vanzaremasini.ro</p>
        <p>marianmadalin700@gmail.com</p>
        <p>gabibarladianu@gmail.com</p>
      </div>
    </div>

    <!-- HARTA GOOGLE MAPS -->
    <div id="map"></div>

    <form id="contactForm">
      <input type="text" id="nume" placeholder="Nume complet" required>
      <input type="email" id="email" placeholder="Adresa de email" required>
      <textarea id="mesaj" placeholder="Mesajul tău..." required></textarea>
      <button type="submit">Trimite mesajul</button>
    </form>
  </div>

  <footer>
    <p>&copy; 2025 Vanzare Masini. Toate drepturile rezervate.</p>
  </footer>

  <script>
    // Inițializează harta Google
    function initMap() {
      const locatie = { lat: 45.653230337967805, lng: 25.58295357464681 }; // coordonate Brașov
      const map = new google.maps.Map(document.getElementById("map"), {
        zoom: 15,
        center: locatie,
        styles: [
          { elementType: "geometry", stylers: [{ color: "#1e1e1e" }] },
          { elementType: "labels.text.stroke", stylers: [{ color: "#1e1e1e" }] },
          { elementType: "labels.text.fill", stylers: [{ color: "#ffffff" }] },
          {
            featureType: "road",
            elementType: "geometry",
            stylers: [{ color: "#2a2a2a" }]
          },
          {
            featureType: "water",
            elementType: "geometry",
            stylers: [{ color: "#000000" }]
          }
        ]
      });

      const marker = new google.maps.Marker({
        position: locatie,
        map: map,
        title: "VanzareMasini - Str. Memorandumului nr.18"
      });
    }

    document.getElementById("contactForm").addEventListener("submit", function(e) {
      e.preventDefault();
      const nume = document.getElementById("nume").value.trim();
      const email = document.getElementById("email").value.trim();
      const mesaj = document.getElementById("mesaj").value.trim();

      if (nume && email && mesaj) {
        alert("Mulțumim, " + nume + "! Mesajul tău a fost trimis cu succes.");
        this.reset();
      } else {
        alert("Te rugăm să completezi toate câmpurile!");
      }
    });
  </script>

  <!-- Script Google Maps API cu cheia ta -->
  <script async
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCjuntapyIR2l9fW_QFKoCPzUh327ppUDQ&callback=initMap">
  </script>
</body>
</html>
